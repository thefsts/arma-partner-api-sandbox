import { getIntegrationReadiness } from './_integrationSecurity.js';
function sendJson(res,status,body){res.statusCode=status;res.setHeader('Content-Type','application/json');res.setHeader('Cache-Control','no-store');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Referrer-Policy','no-referrer');res.setHeader('X-Frame-Options','DENY');res.end(JSON.stringify(body));}
export default function handler(req,res){if(req.method!=='GET'){res.setHeader('Allow','GET');return sendJson(res,405,{error:'METHOD_NOT_ALLOWED'});}const readiness=getIntegrationReadiness();const ready=readiness.gatewayConfigured&&readiness.receiptSigningConfigured&&readiness.processorConfigured&&!readiness.killSwitchActive;return sendJson(res,ready?200:503,{service:'law-shield-arma-integration',ready,...readiness,checkedAt:Date.now()});}
